"""Виды инструментов — единственный на проект словарь названий.

`Instrument.kind` хранит одно из перечисленных ниже значений. Их и только их
понимают обе стороны, которые от вида зависят:

* `ENGINE_MARKET_BY_KIND` в `app/marketdata/service.py` — по виду выбирается
  движок и рынок MOEX, на котором искать котировку;
* `CLASS_BY_KIND` в `app/analytics/service.py` — по виду считается класс актива
  в разбивке портфеля.

Коннекторы переводят сюда свои сырые обозначения (у Т-Банка это имя списочного
метода справочника и поле `instrumentType`), а не заводят собственные названия
видов: раньше вид терялся на стыке коннектора и домена именно потому, что
общего словаря не было и каждая сторона называла вещи по-своему.
"""

SHARE = "share"
BOND = "bond"
ETF = "etf"
CURRENCY = "currency"
FUTURES = "futures"
METAL = "metal"

# Вид, которого мы не знаем: справочник брокера отдал что-то, чему у нас нет
# соответствия. Именно "other", а не молчаливое "share": ложно записанная
# акцией облигация ищется не на том рынке MOEX и попадает не в тот класс
# активов, и заметить это по данным невозможно.
OTHER = "other"

ALL = frozenset({SHARE, BOND, ETF, CURRENCY, FUTURES, METAL, OTHER})
